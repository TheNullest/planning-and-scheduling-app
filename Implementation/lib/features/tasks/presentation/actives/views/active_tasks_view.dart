import 'package:flutter/material.dart';

class ActiveTasksView extends StatelessWidget {
  const ActiveTasksView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('ActiveTasks'),
      ),
    );
  }
}
