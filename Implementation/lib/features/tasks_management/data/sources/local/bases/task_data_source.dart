import 'package:zamaan/data/sources/base_data_source.dart';

abstract class TaskLocalDataSource<Model> extends BaseDataSource<Model> {
  // EResultFuture<List<Model>> getByStatus(TaskStatus status);
  // EResultFuture<List<Model>> getArchived();
  // EResultFuture<List<Model>> getByPriority(Priority priority);
  // EResultFuture<List<Model>> getByDueDateRange(DateTime dueDateFrom, DateTime dueDateTo);
  // EResultFuture<List<Model>> getByCategories(List<String> categoryIds);
  // EResultFuture<List<Model>> getByFixedTags(List<String> tagIds);
}
