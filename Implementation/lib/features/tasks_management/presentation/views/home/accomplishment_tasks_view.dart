import 'package:flutter/material.dart';

class AccomplishmentTasksView extends StatelessWidget {
  const AccomplishmentTasksView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Accomplishments View'),
      ),
    );
  }
}
